# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# AgentOS local state & secrets (never commit)
.agentos/
config.json
*.db
soul.md
wallpapers/
# ...except the ones we ship: those are source, not local state. (Trailing
# comments are not comments in .gitignore — this has to be its own line.)
!agentos/ui/assets/wallpapers/
snapshots/

# Packaging artifacts
packaging/build/
packaging/build-desktop/
packaging/dist/

# Debug artifacts
.playwright-mcp/

# OS / editor
.DS_Store
*.swp
